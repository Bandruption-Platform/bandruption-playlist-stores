# Force legacy peer deps resolution to handle compatibility issues
legacy-peer-deps=true

# Disable npm update notifications in CI
update-notifier=false

# Use exact versions for better reproducibility
save-exact=true

# Disable package-lock.json changes in CI
package-lock=true

# Set npm loglevel
loglevel=warn